spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  application:
    name: meetingmate

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: "f690724f236a88d3b59462f3463e1382"
            client-secret: "QgIOaJBGNTCcrvonxM0vAkZuyrbLisbF"
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
            client-name: Kakao

        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  datasource:
    url: jdbc:mysql://localhost:3306/meetingmate?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: dbswnd2001
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8080

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG



    app:
      jwt:
        issuer: meetingmate
        secret: "CHANGE_ME_TO_32+_BYTES_SECRET_CHANGE_ME_123456"
        access-exp-min: 30
        refresh-exp-day: 14
      oauth2:
        dev-callback: "http://localhost:8080/oauth2/callback"